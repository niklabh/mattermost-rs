// The licensed Go oracle — see main.go. Built only through scripts/go-licensed.sh, which puts this
// module in a go.work beside the pinned server so the server's own go.mod/go.sum resolve every
// dependency; nothing is required here that the workspace does not already provide.
module licensed

go 1.26.4
